export const no_of_notices = 10

export const star_keyword = 'star'

export const unstar_keyword = 'unstar'

// Only ever sent to the API alongside a keyword, so this is the order a search
// arrives in, not the order of the notice list
export const default_sort_mode = 'relevance'
